function tellFortune() {
  let numChildren = 2;
  let partnerName = "Angel Kenshial";
  let location = "Austria";
  let jobTitle = "Full Stack Developer";

  console.log(
    `You will be a ${jobTitle} in ${location}, and married to ${partnerName} with ${numChildren} kids.`,
  );
}
